package ${basePackage}.${packageName}.dao;

import ${basePackage}.${packageName}.entity.${entityName}Entity;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;

/**
 *
 * @author Hsiong
 * @version 1.0.0
 * @since 2022/8/17
 */
public interface ${entityName}Dao extends JpaRepository<${entityName}Entity,Long>, JpaSpecificationExecutor<${entityName}Entity> {
    
}
